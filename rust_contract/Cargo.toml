[package]
name = "rust_contract"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "rust_contract"
path = "src/lib.rs"
crate-type = ["cdylib"]
