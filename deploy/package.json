{
  "name": "gas-whisperer-deploy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy:arbitrum": "hardhat run --network arbitrum scripts/deploy.js",
    "deploy:goerli": "hardhat run --network arbitrumGoerli scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.6.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
